name: tanzania_regions
description: A Flutter package for both android and iOS which provides Regions and its Districts of Tanzania
version: 0.0.2
homepage: https://github.com/MrOne-inc/Tanzania_Regions

environment:
  sdk: '>=3.0.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter: